name: CD

on:
  push:
    branches: [develop, main]

permissions:
  contents: read

# develop -> homolog, main -> prod
jobs:
  deploy:
    name: Deploy API Gateway
    runs-on: ubuntu-latest
    environment: ${{ github.ref_name }}
    concurrency:
      group: os-management-gateway-${{ github.ref_name }}
      cancel-in-progress: false
    defaults:
      run:
        working-directory: terraform
    env:
      TF_VAR_environment: shared
      TF_VAR_origin_url: ${{ secrets.ORIGIN_URL }}
      # Org-level values, MAIN/DEVELOP-suffixed, selected by branch (main->MAIN, develop->DEVELOP).
      TF_STATE_BUCKET: ${{ github.ref_name == 'main' && vars.TF_STATE_BUCKET_MAIN || vars.TF_STATE_BUCKET_DEVELOP }}
      TF_VAR_lambda_state_bucket: ${{ github.ref_name == 'main' && vars.TF_STATE_BUCKET_MAIN || vars.TF_STATE_BUCKET_DEVELOP }}
      TF_VAR_aws_account_id: ${{ github.ref_name == 'main' && vars.AWS_ACCOUNT_ID_MAIN || vars.AWS_ACCOUNT_ID_DEVELOP }}
      AWS_REGION: ${{ vars.AWS_REGION || 'us-east-1' }}
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ github.ref_name == 'main' && secrets.AWS_ACCESS_KEY_ID_MAIN || secrets.AWS_ACCESS_KEY_ID_DEVELOP }}
          aws-secret-access-key: ${{ github.ref_name == 'main' && secrets.AWS_SECRET_ACCESS_KEY_MAIN || secrets.AWS_SECRET_ACCESS_KEY_DEVELOP }}
          aws-region: ${{ env.AWS_REGION }}

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.9.8"

      - name: Validate Terraform state bucket config
        run: |
          if [ -z "${TF_STATE_BUCKET}" ]; then
            echo "TF_STATE_BUCKET is not configured. Set repository variable TF_STATE_BUCKET or secret TF_STATE_BUCKET."
            exit 1
          fi

      - name: Validar state remoto do Lambda
        shell: bash
        run: |
          LAMBDA_STATE_KEY="lambda/shared/terraform.tfstate"

          if aws s3api head-object \
            --bucket "${TF_STATE_BUCKET}" \
            --key "${LAMBDA_STATE_KEY}" \
            --region "${AWS_REGION}" >/dev/null 2>&1; then
            echo "State do Lambda encontrado:"
            echo "s3://${TF_STATE_BUCKET}/${LAMBDA_STATE_KEY}"
          else
            echo "::warning::State remoto do Lambda não encontrado."
            echo "::warning::É necessário executar primeiro o projeto os-management-lambda e garantir que ele salve o state em: s3://${TF_STATE_BUCKET}/lambda/shared/terraform.tfstate"
            exit 1
          fi

      - name: Terraform init
        run: |
          terraform init -input=false \
            -backend-config="bucket=${TF_STATE_BUCKET}" \
            -backend-config="key=gateway/shared/terraform.tfstate" \
            -backend-config="region=${AWS_REGION}" \
            -backend-config="encrypt=true"

      - name: Terraform apply
        run: |
          terraform apply \
            -auto-approve \
            -input=false \
            -var="aws_region=${AWS_REGION}"

      - name: Show gateway URL
        run: |
          echo "auth endpoint: $(terraform output -raw auth_endpoint)"